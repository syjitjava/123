<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.userDao">
    <!--<resultMap id="UserMap" type="user">
        <result column="uid" property="uid"></result>
        <result column="name" property="name"></result>
        <result column="password" property="password"></result>
        <result column="email" property="email"></result>
        <result column="birthday" property="birthday"></result>

        <collection property="accounts" ofType="account" select="com.itheima.dao.AccountDao.findByUid" column="uid">
        </collection>

    </resultMap>
    <resultMap id="resultMap" type="user">
        <result column="uid" property="uid"></result>
        <result column="name" property="name"></result>
        <result column="password" property="password"></result>
        <result column="email" property="email"></result>
        <result column="birthday" property="birthday"></result>
        <collection property="roles" ofType="role" select="findByWithUid" column="uid">
        </collection>

    </resultMap>
    <select id="findAllRole" resultMap="resultMap">
        SELECT * from user 
    </select>

    <select id="findByWithUid" resultType="com.itheima.domain.Role">
        SELECT * from user_role ur LEFT JOIN role r on ur.rid=r.rid WHERE ur.uid=#{uid}
    </select>

<select id="findAllWithAccount" resultMap="UserMap">
    SELECT  * from user
</select>-->
   <!-- <select id="findByUid" resultType="user">
        SELECT  * from user WHERE uid= #{uid}
    </select>-->
</mapper>